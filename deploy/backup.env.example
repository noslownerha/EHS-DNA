# Copy to backup.env and fill in. backup.env is git-ignored (secrets/config stay off-repo).
#   cp deploy/backup.env.example /home/ehs-platform/deploy/backup.env
RCLONE_REMOTE=b2
B2_BUCKET=ehsdna-backups
REMOTE_RETENTION_DAYS=365
